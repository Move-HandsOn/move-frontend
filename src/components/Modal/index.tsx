import ModalClose from "./ModalClose";
import ModalRoot from "./ModalRoot";
import ModalTitle from "./ModalTitle";

export const Modal = {
    Root: ModalRoot,
    Title: ModalTitle,
    Close: ModalClose,
}